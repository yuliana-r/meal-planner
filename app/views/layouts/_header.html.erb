<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><img src="/assets/logo1.png"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <%= link_to "Home", recipes_path, class:"nav-link text-light" %>
        </li>
        <% if user_signed_in? %>
        <% if current_user.id == 1%>
          <li class="nav-item">
            <%= link_to "New Recipe", new_recipe_path, class:"nav-link text-light" %>
          </li>
          <li class="nav-item">
            <%= link_to "New Category", new_category_path, class:"nav-link text-light" %>
          </li>
          <% else %>
          <li class="nav-item">
            <%= link_to "My mealplan", new_mealplan_path, class:"nav-link text-light" %>
          </li>
          <% end %>
          <li class="nav-item">
            <%= link_to "Edit profile", edit_user_registration_path, method: :delete,
           class:"nav-link text-light" %>
          </li>
          <li class="nav-item">
            <%= button_to "Sign out", destroy_user_session_path, method: :delete, class:"nav-link text-light"%>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class:"nav-link text-light" %>
          </li>
          <li class="nav-item">
            <%= link_to "Log in", new_user_session_path, class:"nav-link text-light" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
